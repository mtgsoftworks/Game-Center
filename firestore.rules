rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tüm koleksiyonlarda authenticated kullanıcılar için okuma ve yazma izni
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 